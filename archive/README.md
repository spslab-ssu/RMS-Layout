# Archive

`legacy_root/`에는 저장소 정리 전의 구버전 코드, 중복 데이터, 실행 결과를 로컬
안전 백업으로 보존한다. 이 폴더는 `.gitignore` 대상이므로 다음 커밋에는 포함되지 않는다.

현재 연구·실행·테스트에는 사용하지 않는다. 필요한 과거 결과를 확인하거나 이전
BR/NR 구현과 비교할 때만 참조한다. 현재 기준 코드는 저장소 루트의 `Src/`, `Data/`,
`experiments/`, `tests/`에 있다.
